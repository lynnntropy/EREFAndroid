package rs.veselinromic.eref.wrapper;

import rs.veselinromic.eref.wrapper.model.*;

import java.util.ArrayList;
import java.util.List;

public class Main
{
    public static void main(String[] args) throws Exception
    {
        SessionManager.authenticate("12115141", "t357159");

//        System.out.println(Wrapper.getUserProfile().toString());
//
//        List<NewsItem> news = Wrapper.getNews();
//        for (NewsItem newsItem : news) System.out.println(newsItem.toString());
//
//        List<EboardNewsItem> eboardNews = Wrapper.getEboardNews();
//        for (EboardNewsItem eboardNewsItem : eboardNews) System.out.println(eboardNewsItem.toString() + "\n");
//
//        List<EboardResultsItem> eboardResults = Wrapper.getEboardResults();
//        for (EboardResultsItem eboardResultsitem : eboardResults) System.out.println(eboardResultsitem.toString() + "\n");
//
//        List<EboardExampleItem> eboardExamples = Wrapper.getEboardExamples();
//        for (EboardExampleItem eboardExampleItem : eboardExamples) System.out.println(eboardExampleItem.toString() + "\n");

//        List<EboardFinishingWorkItem> eboardFinishingWorkItems = Wrapper.getFinishingWorkItems();
//        for (EboardFinishingWorkItem eboardFinishingWorkItem: eboardFinishingWorkItems) System.out.println(eboardFinishingWorkItem.toString() + "\n");

//        List<EboardMessageListItem> eboardMessageListItems = Wrapper.getEboardMessageList();
//        for (EboardMessageListItem eboardMessageListItem: eboardMessageListItems) System.out.println(eboardMessageListItem.toString() + "\n");

//        List<ScheduleItem> scheduleItems = ScheduleManager.getSchedule();
//        for (ScheduleItem scheduleItem: scheduleItems) System.out.println(scheduleItem.toString());

//        List<Subject> subjects = SubjectsManager.getSubjects();
//        for(Subject subject: subjects) System.out.println(subject.toString());

//        List<Professor> professors = ProfessorFetcher.getProfessorList();
//        for (Professor professor: professors) System.out.println(professor.toString());
    }
}
